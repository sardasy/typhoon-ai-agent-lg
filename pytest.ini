[pytest]
testpaths = tests
asyncio_mode = auto

# Coverage is opt-in via ``pytest --cov`` / ``scripts/check.bat`` so the
# default ``pytest`` invocation stays fast for inner-loop development.
# The .coveragerc file pins source paths and exclusions.
